name: algebraic_types
description: Dart macros for algebraic types
version: 0.0.1
repository: https://github.com/mcmah309/algebraic_types

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
